swagger: "2.0"
info:
  description: "This is an API server to manage user's balance."
  version: "1.0.0"
  title: "Balance API"
  contact:
    email: "takehiro.a27@gmail.com"
host: "sample.com"
basePath: "/v1"
schemes:
  - "https"
  - "http"
paths:
  /balance:
    post:
      summary: "change user's balance"
      description: ""
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - in: "body"
          name: "body"
          description: "Balance "
          required: true
          schema:
            type: "array"
            items:
              $ref: "#/definitions/balance"
      responses:
        "400":
          description: "Bad Request"
        "500":
          description: "internal Server Error"
definitions:
  balance:
    type: "object"
    required:
      - "user_id"
      - "difference"
      - "currency_code"
      - "timestamp"
      - "is_transaction"
    properties:
      user_id:
        type: "string"
        example: "ff113166-fe3f-40a1-8c94-c30f4f765568"
      difference:
        type: "integer"
        example: -3000
      currency_code:
        type: "string"
        example: "JPY"
      timestamp:
        type: "string"
        example: "2006/01/02 15:04:05"
      is_transaction:
        type: "boolean"
        example: false
